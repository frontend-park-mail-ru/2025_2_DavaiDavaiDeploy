services:
  nginx:
    container_name: frontend-stage
    build:
      context: ..
      dockerfile: docker/Dockerfile.stage
      target: nginx
    networks:
      - ddd-network

networks:
  ddd-network:
    external: true
    name: ddd-network
